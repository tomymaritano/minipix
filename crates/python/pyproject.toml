[build-system]
requires = ["maturin>=1.13,<2"]
build-backend = "maturin"

[project]
name = "minipix"
description = "Image compression and conversion: PNG, JPEG, WebP, AVIF. One Rust core, same bytes everywhere."
requires-python = ">=3.9"
license = "MIT OR Apache-2.0"
dynamic = ["version"]

[tool.maturin]
# el wheel recibe minipix/__init__.pyi + py.typed auto-generados por maturin desde minipix.pyi
include = [{ path = "minipix.pyi", format = "sdist" }]
